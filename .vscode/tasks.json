{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Upload to Pico",
            "type": "shell",
            "command": "ninja",
            "args": [
                "upload"
            ],
            "options": {
                "cwd": "${workspaceFolder}/build/rp2040"
            },
            "problemMatcher": []
        }
    ]
}